# Contributing

Thanks for helping improve Group Chat Archaeologist.

## Local Workflow

1. Run `make install-hooks`.
2. Make focused commits using Conventional Commits.
3. Run `make test` and `make smoke` before pushing.

Do not commit private chat exports, `.env` files, API keys, local model credentials, or generated artifacts containing personal data unless they are intentionally anonymized demo data.
